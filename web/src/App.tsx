export function App() {
  return (
    <main>
      <p className="eyebrow">Native lego control plane</p>
      <h1>AcmeMux</h1>
      <p>
        The verified application foundation is ready. Runtime, workspace, and
        certificate features will appear only as their supported boundaries are
        delivered.
      </p>
      <section aria-labelledby="ownership-heading">
        <h2 id="ownership-heading">One authoritative workspace</h2>
        <p>
          Upstream lego will continue to own ACME operations and native account,
          certificate, and private-key artifacts.
        </p>
      </section>
    </main>
  );
}
